Task 1 – Real-time connection

        Goal: Prove that the server and browser can talk in real-time with low delay.

        Input: A simple test message from the browser (like “ping”).

        Process: Server receives the message and sends a reply (“pong”).

        Output: Browser shows how long it took (delay in milliseconds).

        Success: Delay is small (well under 2 seconds), stable.